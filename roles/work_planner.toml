id = "work_planner"
description = "Decomposes validated requirements into dependency-aware execution units."
permission_level = "workspace-write"
owned_skills = ["decomposition"]
forbidden_actions = ["select execution targets", "implement product changes", "approve gates", "redesign requirements"]

[scope]
stage = "decomposition"
instruction = "Produce bounded work units with ownership, dependencies, allowed files, acceptance criteria, and validation needs."
